let reserva = prompt("Ingrese que tipo de habitacion desea separar: Individual, doble y familiar")
let personas= prompt("Ingrese el numero de personas que se van a hospedar")
let fumadores
let noFumadores
let resultado
let individual
let doble
let familiar


switch (reserva) {
    case 1:
        if(individual < 2);
        {
            resultado("La reserva se ha realizado exitosamente")
        }
           break;

    default:
        break;
}
